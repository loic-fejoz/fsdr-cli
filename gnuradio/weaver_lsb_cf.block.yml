id: weaver_lsb_cf
label: Weaver LSB (FutureSDR)
category: '[FutureSDR]'

parameters:
- id: audio_freq
  label: Audio Freq Rate
  dtype: float
  default: '1500/48000'

inputs:
- domain: stream
  dtype: complex

outputs:
- domain: stream
  dtype: float

file_format: 1
